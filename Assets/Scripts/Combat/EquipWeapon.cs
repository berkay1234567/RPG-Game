using System.Collections;
using System.Collections.Generic;
using Unity.VisualScripting;
using UnityEngine;

public class EquipWeapon
{


 /*   public bool weaponEquipped { get; private set; }

     EquipmentSystem equipmentSystem;


    public void DrawWeapon(Animator animator)
    {
        equipmentSystem =animator.GetComponent<EquipmentSystem>();
        if (!weaponEquipped && !equipmentSystem.weaponOnhand)
        {
            animator.SetTrigger("DrawWeapon");
            weaponEquipped = true;
        }        
    }

    public void UnDrawWeapon(Animator animator)
    {
        equipmentSystem = animator.GetComponent<EquipmentSystem>();
        if (weaponEquipped && equipmentSystem.weaponOnhand) {
            animator.SetTrigger("UndrawWeapon");
            weaponEquipped = false;
            
        }
 
    }*/
}
